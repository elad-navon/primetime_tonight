# מה יש בפריים טיים?

דף אינטרנט להצגת לוח השידורים של ערוצי הטלוויזיה המרכזיים וספורט 5, עם
דגש על תוכניות הפריים טיים.

הדף מושך את נתוני השידורים בזמן אמת מ-API של yes ומציג אותם בממשק כהה,
מודרני ורספונסיבי.

## מה הדף מציג?

-   פריים טיים בין 21:00 ל-24:00 (בערוצי הספורט: החל מ-18:00)
-   לוח שידורים מלא עד חצות
-   בחירת יום להצגת לוח עתידי
-   שעון ישראל בזמן אמת
-   סטטוס לכל תוכנית:
    -   משודר עכשיו
    -   הסתיים
    -   תוכנית הבאה
-   פס התקדמות בזמן שידור
-   תמונות ותיאורים של תוכניות כאשר הנתונים זמינים
-   אפשרות לפתוח תוכניות נוספות בתוך כל ערוץ
-   אפשרות לבחור תוכנית לספוטלייט בראש הכרטיס
-   סינון לפי:
    -   כל הערוצים
    -   ערוצים 11, 12, 13
    -   ערוצי ספורט 5
-   הסתרה אוטומטית של מהדורות חדשות בפריים טיים
-   אפשרות להציג מחדש את החדשות
-   רענון ידני
-   רענון אוטומטי של התצוגה בכל דקה
-   משיכת נתונים מחדש מה-API בכל 5 דקות
-   Favicon ואייקון אפליקציה מותאמים אישית (סימניה ירוקה מלאה)
-   אפשרות התקנה כאפליקציה (PWA) על מסך הבית בנייד

## ערוצים

הדף מציג את הערוצים הבאים:

### ערוצים מרכזיים

-   11 - כאן 11
-   12 - קשת 12
-   13 - רשת 13

### ערוצי ספורט

-   55 - ספורט 5
-   56 - ספורט 5+
-   57 - ספורט 5 גולד
-   58 - ספורט 5 לייב
-   59 - ספורט 5 סטארס
-   60 - ספורט 5 מקס

## מקור הנתונים

הנתונים נמשכים בזמן אמת מה-API של yes:

`https://svc.yes.co.il/api/content/broadcast-schedule/channels/`

לכל ערוץ מתבצעת בקשה נפרדת לפי התאריך הנבחר.

הדף משתמש ב-API ישירות מהדפדפן, ולכן נדרש חיבור לאינטרנט בזמן השימוש.

## אזור זמן

כל הזמנים בדף מוצגים לפי:

`Asia/Jerusalem`

הקוד מטפל באופן מפורש בהמרת תאריכים ושעות בין שעון ישראל ל-UTC, כדי
שהשעות יוצגו נכון גם אם המחשב נמצא באזור זמן אחר.

## מבנה הפרויקט

עיקר האפליקציה בנוי כקובץ HTML יחיד:

``` text
index.html
```

הקובץ כולל בתוכו HTML, CSS, JavaScript ו-Favicon מוטמע (בגודל קטן).

לצידו נמצאים קבצים נוספים הדרושים להתקנה כאפליקציה (PWA):

``` text
manifest.json      – הגדרות ה-PWA (שם, צבעים, אייקונים)
sw.js              – Service Worker בסיסי, נדרש כדי שהדפדפן יאפשר התקנה
icons/             – אייקוני האפליקציה בגדלים שונים, כולל גרסת maskable
TVAVATAR.jpg       – תמונת ההשראה לעיצוב הלוגו (לא בשימוש ישיר בבנייה)
```

אין צורך בשרת Backend כדי להפעיל את הממשק, אך התקנה אמיתית כאפליקציה
בנייד (ראו בהמשך) דורשת אחסון בכתובת HTTPS.

## הפעלה

אפשר לפתוח את קובץ ה-HTML ישירות בדפדפן.

מומלץ להשתמש ב-Chrome או בדפדפן מבוסס Chromium.

מכיוון שהדף פונה ל-API חיצוני של yes, סביבת Preview מסוימת עשויה להציג
בקשת הרשאה לגישה לרשת.

במקרה כזה יש לאפשר את החיבור לכתובות של `svc.yes.co.il`.

## Favicon ואייקון האפליקציה

עיצוב אחיד לכל הגדלים: סימניה (Bookmark) מלאה בירוק רגיל (`#22C55E`),
על רקע שקוף. מצוירת ישירות בקוד (לא תמונה מוטמעת).

-   `icon-16.png` עד `icon-512.png` – הסימניה הירוקה על רקע שקוף
    לגמרי, לשימוש כ-Favicon וכאייקון "רגיל" של ה-PWA.
-   `icon-maskable-512.png` ו-`apple-touch-icon.png` – אותה סימניה
    ירוקה על רקע שקוף, בלי שום צבע רקע מלאכותי שמתווסף — בכוונה. אלה
    האייקונים שמופיעים בפועל במסך הבית באנדרואיד וב-iOS. בגרסה קודמת
    ניסינו למלא כאן רקע כהה אחיד (כדי שהסימניה תישאר ברורה על אנדרואיד,
    שגוזר את האייקון לעיגול/ריבוע מעוגל לפי הלאנצ'ר), אבל זה יצר "ריבוע
    שחור" מיותר סביב האייקון — אז הוסר. הסימניה עדיין מוקטנת ומורחקת
    מהקצוות כדי שלא תיחתך בגזירה, אבל בלי רקע מאחוריה. שימו לב: ב-iOS
    ספציפית, Safari ממלא שקיפות בשחור אוטומטית בעצמו (התנהגות קבועה של
    המערכת, לא משהו שהקובץ עושה) — זו מגבלה של הפלטפורמה ולא ניתנת
    לעקיפה מהצד שלנו.

גרסת ה-32×32 מוטמעת ישירות בתוך `index.html` באמצעות Base64 עבור
הפאביקון בכרטיסיית הדפדפן; שאר הגדלים נטענים כקבצים נפרדים מתיקיית
`icons/`.

## התקנה כאפליקציה בנייד (PWA)

הדף כולל `manifest.json` ו-`sw.js` (Service Worker) שמאפשרים להתקין
אותו כאפליקציה על מסך הבית בנייד, במקום רק לפתוח אותו כדף אינטרנט.

ה-Service Worker עובד בגישת "רשת קודם" – בכל טעינה הוא מנסה להביא
גרסה עדכנית מהשרת, ורק אם אין בכלל חיבור לרשת הוא נופל חזרה לעותק
השמור מהפעם הקודמת. כך עדכונים לדף (כמו שינוי באייקון) אמורים להופיע
כמעט מיד בפעם הבאה שפותחים את האפליקציה, בלי צורך למחוק ולהתקין
מחדש.

כדי שהתקנה כזו תעבוד בפועל צריך שהדף יוגש מכתובת מאובטחת (HTTPS) –
לדוגמה באמצעות GitHub Pages על הריפו הזה. פתיחה ישירה של הקובץ מהדיסק
(`file://`) לא מספיקה לרוב הדפדפנים כדי להציג אפשרות התקנה.

לאחר שהדף פעיל בכתובת HTTPS:

-   **אנדרואיד (Chrome):** נכנסים לתפריט ⋮ ובוחרים "התקן אפליקציה" /
    "הוסף למסך הבית".
-   **iPhone (Safari):** לוחצים על כפתור השיתוף ובוחרים "הוסף למסך
    הבית".

לאחר ההתקנה האפליקציה נפתחת במסך מלא, עם האייקון והשם שהוגדרו
ב-`manifest.json`, אך עדיין מושכת את הנתונים בזמן אמת מה-API של yes.

### עדכון אייקון בקיצור שכבר נוסף למסך הבית

גם אנדרואיד וגם iOS (וגם דפדפנים כמו Chrome/Brave באופן כללי) נוטים
לשמור עותק של קובץ ה-manifest ושל האייקונים במטמון פנימי משלהם,
בנפרד מה-HTTP הרגיל של הדף. זה קורה **גם בהתקנה חדשה לגמרי** של
הקיצור, לא רק כשמרעננים אחד קיים — אם ה-manifest עצמו נשאר באותה
כתובת מדויקת, הדפדפן עלול להגיש עותק ישן שלו מהמטמון גם כשמנסים
להתקין "מההתחלה", ואז ההתקנה החדשה מקבלת בפועל את רשימת האייקונים
הישנה שהיתה כתובה בתוכו.

הדרך האמינה להתגבר על זה היא לצרף פרמטר גרסה (`?v=`) **גם לכתובת של
ה-manifest עצמו, וגם** לכתובת כל קובץ אייקון — כל אחד מהם הופך
טכנית לכתובת חדשה, אז הדפדפן חייב להוריד את שניהם מחדש ולא יכול
"להיתקע" על עותק ישן של אף אחד מהם. לכן יש `?v=3` בשלושה מקומות:
קישור ה-`manifest` ב-`index.html`, קישור ה-`apple-touch-icon`
ב-`index.html`, והאייקונים בתוך `manifest.json` עצמו.

**בכל פעם שמחליפים את קובץ האייקון בעתיד, יש להעלות את המספר הזה
בכל שלושת המקומות (למשל ל-`?v=4`)** — אחרת שינוי בתוכן הקבצים בלבד
עלול לא להיראות, גם בהתקנה חדשה של הקיצור למסך הבית.

## סינון חדשות

הדף מזהה חדשות לפי כותרת התוכנית.

הזיהוי מבוסס על מילות מפתח כגון:

-   חדשות
-   מהדורה
-   מבזק
-   כותרות הבוקר

בגלל שה-API אינו מספק שדה Genre ייעודי, הסינון מתבצע לפי הכותרת.

בערוצי הספורט, "חדשות הספורט" מוחרגות מהסינון ונשארות מוצגות.

## רענון

הדף מבצע שני סוגי רענון:

### רענון תצוגה

פעם בדקה.

משמש לעדכון:

-   השעה
-   סטטוס התוכנית
-   פס ההתקדמות
-   תוכנית שמשודרת כרגע

### רענון נתונים

כל 5 דקות.

בשלב זה מתבצעת שליפה מחדש של נתוני הערוצים מה-API.

## טיפול בשגיאות

אם ערוץ מסוים לא נטען, הדף ממשיך לעבוד ומציג הודעת שגיאה עבור הערוץ
שנכשל.

כך תקלה בערוץ אחד אינה מונעת הצגה של שאר הערוצים.

## טכנולוגיות

הפרויקט משתמש בטכנולוגיות Web סטנדרטיות:

-   HTML5
-   CSS3
-   JavaScript
-   Fetch API
-   Intl.DateTimeFormat
-   CSS Grid
-   Flexbox
-   Responsive Design

אין שימוש בספריות JavaScript חיצוניות.

## רישוי ומקור הנתונים

הפרויקט הוא ממשק צד לקוח המשתמש בנתוני שידורים שמתקבלים מ-yes.

יש להתייחס ל-API ולנתונים בהתאם לתנאי השימוש והזמינות של השירות.

## הערה

הדף תלוי בזמינות ה-API של yes. אם השירות אינו זמין, השתנה מבנה ה-API או
נחסמה הגישה אליו, ייתכן שהנתונים לא ייטענו.
